name: openat

on:
  pull_request:
  push:

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    - uses: ./.github/actions/msdevshell
    - run: |
        cl openat.c
